// css

When the user clicks on title input, it will focus, having the word become bold.
When the user exists without input, focusOUT will make sure the line item
will turn red indicating that nothing was inputted

it will turn green unless the check of an if statement or focusout even checks
turning it green


<div class="alert alert-success">
<strong>Success!</strong> Indicates a successful or positive action.
</div>

^^ for when the form is submitted! (bootstrap)

-----------------

ajax -- autocomplete




php ----

track ip by submitted.
redirect after submitted complete bug

MAIN WILL HAVE BUTTONS TO DELETE A BUG, EDIT A BUG,


module to create new users via input forms.


SQL --

give id auto increment. use values as VALUES(NULL) so it will auto increment
automatically.
